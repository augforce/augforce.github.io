source "https://rubygems.org"

# GitHub Pages builds the live site with this gem. It's only needed locally
# for previewing with `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins
